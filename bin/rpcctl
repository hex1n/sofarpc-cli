#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
JAR_PATH="${ROOT_DIR}/target/rpcctl-launcher.jar"

if [[ ! -f "${JAR_PATH}" ]]; then
  echo "Missing ${JAR_PATH}. Run ./scripts/build.sh first." >&2
  exit 1
fi

exec java -jar "${JAR_PATH}" "$@"
