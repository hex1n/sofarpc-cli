currentContext: test
contexts:
  test:
    description: Example test context using the local project manifest.
    manifestPath: ./rpcctl-manifest.yaml
    runtimeBaseUrl: https://github.com/hex1n/sofa-rpcctl/releases/download/v0.1.0
  direct-local:
    description: Example direct invocation context.
    directUrl: bolt://127.0.0.1:12200
    sofaRpcVersion: 5.4.0
